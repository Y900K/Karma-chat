const production=process.argv.includes("--production"),required=["NEXT_PUBLIC_SUPABASE_URL","NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY","NEXT_PUBLIC_SITE_URL","NVIDIA_API_KEY","NVIDIA_MODEL","NVIDIA_EMBEDDING_MODEL","SUPABASE_SERVICE_ROLE_KEY","CRON_SECRET"],missing=required.filter(k=>!process.env[k]);if(production&&missing.length){console.error(`Missing production variables: ${missing.join(", ")}`);process.exit(1)}if(process.env.NEXT_PUBLIC_SITE_URL?.includes("localhost")&&production){console.error("NEXT_PUBLIC_SITE_URL must be the production HTTPS origin.");process.exit(1)}for(const name of ["NVIDIA_API_KEY","SUPABASE_SERVICE_ROLE_KEY","CRON_SECRET"]){if(process.env[`NEXT_PUBLIC_${name}`]){console.error(`${name} must remain server-only.`);process.exit(1)}}console.log(production?"Production environment validated.":`Environment check complete. Optional local values missing: ${missing.join(", ")||"none"}`);
