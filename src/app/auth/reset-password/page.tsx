"use client";
import { FormEvent,useState } from "react";import Link from "next/link";import { CheckCircle2,LockKeyhole } from "lucide-react";import { createClient } from "@/lib/supabase/client";import "../auth.css";
export default function ResetPassword(){const[message,setMessage]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const password=String(new FormData(e.currentTarget).get("password")||"");if(password.length<10){setMessage("Password must be at least 10 characters.");return}const sb=createClient();if(!sb){setMessage("Supabase is not configured.");return}const{error}=await sb.auth.updateUser({password});setMessage(error?error.message:"Password updated. You can now sign in.")};return <main className="auth-panel" style={{minHeight:"100vh"}}><div className="auth-card"><span className="secure"><LockKeyhole/>Secure recovery</span><h2>Choose a new password.</h2><p>Use at least 10 characters and avoid a password used elsewhere.</p><form onSubmit={submit}><label>New password<span><LockKeyhole/><input type="password" name="password" minLength={10} required autoComplete="new-password"/></span></label>{message&&<div className="auth-success"><CheckCircle2/> {message}</div>}<button className="auth-submit">Update password</button></form><div className="auth-switch"><Link href="/auth">Return to sign in</Link></div></div></main>}
